# Create a variable in the pre/post section with link to a RHEL-9 unified ISO
# The curl tool will download this ISO and it will be processed later in the section.
ISO_LOCATION=<link to a unified ISO>

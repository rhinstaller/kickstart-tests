name: pull request
description: Tests run on GitHub pull requests with supplied query
point_person: rvykydal@redhat.com
artifact_type: github.pr
verified_by:
  test_cases:
    # Specific query should be supplied by run_subset or everything event
    query: 'True'
configurations:
  - architecture: x86_64
